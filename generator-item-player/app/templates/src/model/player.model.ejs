import { ItemPlayerModel, EventMgr } from "libs-base-item-player";

class <%= title %>Model extends ItemPlayerModel {
  constructor(itemJSON: object, eventMgr: EventMgr) {
    super(itemJSON, eventMgr);
  }

  // Add getters and setters for your own itemJSON.
}

export default <%= title %>Model;
